curl --verbose \
    -X POST http://localhost:8080/restaurants 
    -H 'Content-Type: application/json' \
    --data-raw '[
                 {"name":"taste the difference",
                  "menus":" "}
                ]'
